# Configuration Frontend (.env)

Crée un fichier `.env.local` dans `web/` si tu veux changer l'URL de l'API.

Exemple:

```
VITE_API_URL=http://localhost:5000/api
```

Par défaut, si ce fichier n'existe pas, l'app utilise `http://localhost:5000/api`.
